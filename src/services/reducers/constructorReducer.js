const initialState = {
    constructorIngredients: [],
};

export default function constructorReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
