const initialState = {
    ingredientsList: [],
    currentIngredient: {}
};

export default function ingredientsReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
